import { SafeAreaView } from "@/components/SafeAreaView";
import { Text } from "react-native";

const Cars = () => {
  return (
    <SafeAreaView>
      <Text>Cars</Text>
    </SafeAreaView>
  );
};

export default Cars;
