import { SafeAreaView } from "@/components/SafeAreaView";
import { Text } from "react-native";

const Bills = () => {
  return (
    <SafeAreaView>
      <Text>Bills</Text>
    </SafeAreaView>
  );
};

export default Bills;
